node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local environment files
.env
.env.*

# OS/editor noise
.DS_Store
Thumbs.db
.vscode/
.claude/settings.local.json

# Build output
dist/
build/

# Checkout of the gh-pages branch (the published site), from prototype: npm run deploy:pages
.gh-pages/

# Local site backups (large zips kept out of git; git tag pre-redesign-2026-06 is the canonical restore point)
backups/

# Store backend: local database + uploaded product images (runtime data, not source)
server/store.db
server/store.db-*
server/uploads/
